# Native simulator demo

This migrated native-core program demonstrates BOOL, DINT, REAL, TIMER, COUNTER, latch, comparison, arithmetic and branch execution in C. Run the deterministic scenario with `rsmicro run-native project.rsmproj --controller controller-a --scenario scenario.json --format json`.
