add_library(rsmlink STATIC src/rsm_link_frame.c src/rsm_link_stream.c)
target_include_directories(rsmlink PUBLIC include ${CMAKE_CURRENT_SOURCE_DIR}/../generated)
target_compile_features(rsmlink PUBLIC c_std_99)
if(RSM_ENABLE_STRICT_WARNINGS)
 target_compile_options(rsmlink PRIVATE -Wall -Wextra -Werror -Wpedantic)
endif()
if(RSM_BUILD_TESTS)
 add_executable(rsmlink_tests tests/test_frame.c)
 target_link_libraries(rsmlink_tests PRIVATE rsmlink)
 add_test(NAME rsmlink_tests COMMAND rsmlink_tests)
endif()
