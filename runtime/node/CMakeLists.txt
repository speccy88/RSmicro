add_executable(rsmnode src/main.c src/command_queue.c)
set_target_properties(rsmnode PROPERTIES OUTPUT_NAME rsm-node)
target_include_directories(rsmnode PRIVATE include)
target_link_libraries(rsmnode PRIVATE rsmcore rsmlink)
target_compile_features(rsmnode PRIVATE c_std_99)
if(RSM_ENABLE_STRICT_WARNINGS)
 target_compile_options(rsmnode PRIVATE -Wall -Wextra -Werror -Wpedantic)
endif()
if(RSM_BUILD_TESTS)
 add_executable(rsmnode_tests tests/test_queue.c src/command_queue.c)
 target_include_directories(rsmnode_tests PRIVATE include)
 add_test(NAME rsmnode_tests COMMAND rsmnode_tests)
endif()
