# Task 3 baseline

* Starting commit: `f7dac0aa2dfb40bb7bde45b2b3778302976a3c7e` (clean work tree).
* Editable installation: blocked because the isolated environment could not download `setuptools>=68` (HTTP 403).
* Baseline pytest: collection failed with ten import errors because installation was unavailable.
* Registry check: passed.
* Profile: RSM-LOGIX-CORE-1, version 1.0.0, instruction ABI 1.
* Image: version 1.0, little endian; fourteen original sections (tags, values, timer/counter layouts, tasks, routines, rungs, instruction/state streams, produced/consumed tags, debug/string tables, estimates).
* Mandatory instructions (24): XIC, XIO, OTE, OTL, OTU, ONS, TON, CTU, CTD, RES, EQ, NE, GT, GE, LT, LE, MOV, CLR, ADD, SUB, MUL, DIV, NEG, ABS.
* Conformance fixtures: 24.
* Assumptions found: CRC is IEEE CRC-32 (not CRC32C); tag/rung metadata was JSON; branch lanes are flattened during lowering; endpoint IDs were absent. The runtime retains 1.0 compatibility but requires the new binary runtime metadata sections generated by the compiler.
* Expected API: arena sizing/init, validation/loading, lifecycle modes, scan, typed tag/member access, force overlays, observational snapshots, diagnostics and faults.
* Preserved runtimes: `plc_ascii`, Python, CircuitPython, MicroPython, and Propeller 2/TAQOZ.
