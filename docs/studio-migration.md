# Studio migration

`rsmicro-studio` is the recommended engineering entry point. It opens canonical projects and adds Qt model/view, graphics editing and shared SCADA rendering. `plc-ascii`, `plc-ascii-cli` and `plc-runtime` remain unchanged and available. No source format downgrade occurs. Hardware runtime migration and physical validation remain unverified; Task 8 performs final repository-wide integration.
