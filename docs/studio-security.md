# Studio security

Studio defaults controller and broker definitions to loopback. RSM Link 1.0 has no authentication or encryption; use only trusted local networks. Local roles are policy labels, not secure authentication. Projects and screens cannot execute scripts. Forces and engineering writes require deliberate workflows and are not source values. Studio is not safety-certified software; external physical interlocks remain mandatory.
