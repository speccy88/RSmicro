# ADR 0004: UUID identity and local IDs

Accepted. Stable UUIDs remain in manifests/maps while dense deterministic IDs make runtime tables compact. Stateful instructions receive controller-global IDs and unique state slots.
