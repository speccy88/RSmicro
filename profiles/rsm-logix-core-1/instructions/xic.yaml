{
  "mnemonic": "XIC",
  "display_name": "XIC",
  "opcode": 1,
  "category": "bit",
  "description": "Examine-if-closed contact; passes rung continuity only when its BOOL operand is true.",
  "source_syntax": "XIC(operand1)",
  "operands": [
    {
      "role": "read_bool",
      "readable": true,
      "writable": false,
      "types": [
        "BOOL"
      ],
      "literal": true,
      "members": true
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "When input power and the BOOL operand are true, preserves rung continuity; does not write an operand or retain state.",
  "false_behavior": "A false BOOL operand blocks rung continuity; does not write an operand or retain state.",
  "prescan": "XIC has no prescan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "postscan": "XIC has no postscan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "state": "none",
  "affected_members": [],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/xic.json"
  ],
  "fixtures": [
    "conformance/xic-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
