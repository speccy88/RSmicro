{
  "mnemonic": "CTD",
  "display_name": "CTD",
  "opcode": 18,
  "category": "counter",
  "description": "Count-down instruction that counts false-to-true rung transitions; ACC is not clamped to PRE.",
  "source_syntax": "CTD(operand1)",
  "operands": [
    {
      "role": "counter",
      "readable": true,
      "writable": true,
      "types": [
        "COUNTER"
      ],
      "literal": false,
      "members": false
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "With rung continuity, sets CD; on a false-to-true transition decrements ACC, wrapping INT32_MIN to INT32_MAX and setting UN; DN is ACC >= PRE.",
  "false_behavior": "Without rung continuity, clears CD and records a false edge baseline; ACC, DN, and UN retain their current values.",
  "prescan": "Prescan clears CU and CD and initializes the transition baseline as true, suppressing a count when the rung is already true on the first active scan.",
  "postscan": "Postscan clears CU and CD and resets the transition baseline; ACC, DN, OV, UN, and PRE are retained.",
  "state": "unique slot required",
  "affected_members": [
    "ACC",
    "CD",
    "DN",
    "UN"
  ],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/ctd.json"
  ],
  "fixtures": [
    "conformance/ctd-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
