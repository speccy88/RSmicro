{
  "mnemonic": "RES",
  "display_name": "RES",
  "opcode": 19,
  "category": "timer_counter",
  "description": "Reset for TIMER or COUNTER tags; preserves PRE and clears accumulated/status state when powered.",
  "source_syntax": "RES(operand1)",
  "operands": [
    {
      "role": "composite",
      "readable": true,
      "writable": true,
      "types": [
        "TIMER",
        "COUNTER"
      ],
      "literal": false,
      "members": false
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "With rung continuity, clears TIMER ACC/EN/TT/DN or COUNTER ACC/CU/CD/DN/OV/UN, preserves PRE, and clears associated TON/CTU/CTD execution state.",
  "false_behavior": "Without rung continuity, performs no reset and does not change the target tag or associated execution state.",
  "prescan": "RES does not execute a reset during prescan; lifecycle initialization of TIMER/COUNTER fields is performed independently of RES rung power.",
  "postscan": "RES does not execute a reset during postscan; lifecycle cleanup of TIMER/COUNTER fields is performed independently of RES rung power.",
  "state": "none",
  "affected_members": [
    "ACC",
    "EN",
    "TT",
    "DN",
    "CU",
    "CD",
    "OV",
    "UN"
  ],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/res.json"
  ],
  "fixtures": [
    "conformance/res-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
