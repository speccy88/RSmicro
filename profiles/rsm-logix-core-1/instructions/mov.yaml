{
  "mnemonic": "MOV",
  "display_name": "MOV",
  "opcode": 48,
  "category": "numeric",
  "description": "Copies source to destination using the profile conversion rules.",
  "source_syntax": "MOV(operand1, operand2)",
  "operands": [
    {
      "role": "numeric",
      "readable": true,
      "writable": false,
      "types": [
        "DINT",
        "REAL"
      ],
      "literal": true,
      "members": true
    },
    {
      "role": "write_numeric",
      "readable": false,
      "writable": true,
      "types": [
        "DINT",
        "REAL"
      ],
      "literal": false,
      "members": false
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "With rung continuity, copies source to destination using the profile conversion rules.",
  "false_behavior": "Without rung continuity, performs no write; the destination retains its prior backing value.",
  "prescan": "MOV has no prescan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "postscan": "MOV has no postscan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "state": "none",
  "affected_members": [],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/mov.json"
  ],
  "fixtures": [
    "conformance/mov-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
