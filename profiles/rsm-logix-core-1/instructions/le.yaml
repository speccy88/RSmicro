{
  "mnemonic": "LE",
  "display_name": "LE",
  "opcode": 37,
  "category": "comparison",
  "description": "Comparison contact; passes rung continuity when operand1 is less than or equal to operand2.",
  "source_syntax": "LE(operand1, operand2)",
  "operands": [
    {
      "role": "numeric",
      "readable": true,
      "writable": false,
      "types": [
        "DINT",
        "REAL"
      ],
      "literal": true,
      "members": true
    },
    {
      "role": "numeric",
      "readable": true,
      "writable": false,
      "types": [
        "DINT",
        "REAL"
      ],
      "literal": true,
      "members": true
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "When input power is true and operand1 <= operand2, preserves rung continuity; does not write a destination or retain state.",
  "false_behavior": "When operand1 is not less than or equal to operand2, blocks rung continuity; does not write a destination or retain state.",
  "prescan": "LE has no prescan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "postscan": "LE has no postscan action: it owns no retained execution state and does not perform an output lifecycle write.",
  "state": "none",
  "affected_members": [],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/le.json"
  ],
  "fixtures": [
    "conformance/le-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
