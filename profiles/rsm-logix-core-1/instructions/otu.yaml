{
  "mnemonic": "OTU",
  "display_name": "OTU",
  "opcode": 5,
  "category": "bit",
  "description": "Retentive output unlatch; clears its BOOL destination only while rung continuity is true.",
  "source_syntax": "OTU(operand1)",
  "operands": [
    {
      "role": "write_bool",
      "readable": false,
      "writable": true,
      "types": [
        "BOOL"
      ],
      "literal": false,
      "members": false
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "With rung continuity, writes false to the BOOL destination and leaves it unlatched after the rung becomes false.",
  "false_behavior": "Without rung continuity, performs no write; the BOOL destination retains its prior backing value.",
  "prescan": "No instruction-specific prescan write; OTU does not alter its destination during lifecycle entry.",
  "postscan": "No instruction-specific postscan write; OTU does not alter its destination during lifecycle exit.",
  "state": "none",
  "affected_members": [],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/otu.json"
  ],
  "fixtures": [
    "conformance/otu-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
