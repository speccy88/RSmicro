{
  "mnemonic": "ONS",
  "display_name": "ONS",
  "opcode": 6,
  "category": "bit",
  "description": "One-shot rising instruction with an explicit, instruction-unique BOOL storage operand.",
  "source_syntax": "ONS(storage_bool)",
  "operands": [
    {
      "name": "storage",
      "kind": "tag",
      "types": [
        "BOOL"
      ],
      "access": "read_write"
    }
  ],
  "rung_condition_in": "consumed",
  "rung_condition_out": "input power AND instruction predicate for contacts/comparisons; otherwise input power",
  "true_behavior": "When input power is true and storage was false, passes one scan of rung continuity and writes storage true; when storage is already true, blocks continuity while retaining storage true.",
  "false_behavior": "When input power is false, blocks rung continuity and writes the storage BOOL false, re-arming the next false-to-true transition.",
  "prescan": "Prescan writes true to the ONS storage BOOL, suppressing a startup pulse if the rung is already true on the first active scan.",
  "postscan": "Postscan does not write ONS storage; its current value is retained until normal scan execution changes it.",
  "state": "explicit BOOL storage operand; legacy zero-operand projects receive a deterministic hidden BOOL tag during lowering",
  "affected_members": [],
  "faults": [
    "invalid operand",
    "invalid state",
    "overflow or conversion where applicable"
  ],
  "conversion": "mixed numeric promotes DINT to binary32 REAL; destination conversion is range checked",
  "overflow": "DINT and non-finite REAL fault with destination unchanged",
  "force": "reads observe force overlay; writes update backing value while effective forced value remains overlaid",
  "multiple_writer": "source scan order; compiler warns for multiple destructive destinations",
  "validation": "exact operand count/types; instruction-owned status members are not general destinations",
  "references": [
    "references/ons.json"
  ],
  "fixtures": [
    "conformance/ons-core.json"
  ],
  "runtime_notes": "Decode explicit operands; no native struct casts; lifecycle and faults must be implemented."
}
